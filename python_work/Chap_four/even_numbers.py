#Using range() to skip numbers in a range of numbers
even_numbers = list(range(2, 11, 2))
print(even_numbers)

#Using the third argument in range() tells you how many numbers to skip
#over in a given range. Thus list(range(2, 11, 3)) will return [2, 5, 8].
